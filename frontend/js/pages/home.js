// Home Page
// Manages home page initialization

function initHomePage() {
  const homePage = document.getElementById('home-page');
  // Home page is already in HTML
  homePage.classList.remove('hidden');
}
